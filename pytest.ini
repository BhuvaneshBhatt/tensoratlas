[pytest]
pythonpath = src
addopts = --assert=plain
markers =
    slow: expensive symbolic or benchmark-adjacent tests
    notebook: notebook execution and notebook-content tests
    plot: tests or smoke checks requiring plotting dependencies
